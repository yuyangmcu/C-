# C 语言编程规范

# 一、总则

本规范旨在统一 C 语言代码编写风格，提升代码可读性、可维护性和可移植性，适用于所有 C 语言项目开发。所有开发人员需严格遵守，特殊情况需经项目组讨论确认后可例外。

# 二、代码格式规范

## 2\.1 缩进与空格

- 缩进统一使用4个空格，禁止使用 Tab 键，避免不同编辑器显示差异。

- 运算符两侧需加空格，如`a = b + c`，但单目运算符（如`!a`、`a++`）两侧不加空格。

- 函数参数列表中，参数之间用逗号分隔，逗号后需加空格，如`void func(int a, char b)`。

- 关键字（如`if`、`for`、`while`）后需加空格，如`if (a > b)`。

## 2\.2 换行与空行

- 一行代码长度建议不超过80个字符，超出需换行，换行后需缩进对齐。

- 函数之间需空一行，函数内部逻辑块之间可根据情况空行分隔，提升可读性。
- 预处理指令（如`#include`、`#define`）单独占一行，前后可空行分隔。

## 2\.3 大括号使用

- 函数体、条件语句、循环语句等的大括号，遵循“左大括号紧跟语句末尾，右大括号单独一行”的原则，示例：
  
    ```c
    if (a > b) {
    	printf("a is greater than b");
    } 
    else {
      	printf("b is greater than a");
    }
    ```
    
    即使语句体只有一行，也必须添加大括号，避免后续修改时出现逻辑错误。

# 三、命名规范

## 3\.1 变量命名

- 变量名采用小写字母，多个单词用下划线分隔，如`user_name`、`max_value`。
- 变量名需具有描述性，能清晰表达变量含义，避免使用无意义的单字符变量（循环变量除外，如`i`、`j`）。
- 全局变量需添加后缀（如`_g`），区分局部变量和全局变量，如 system_status_g。
- 静态全局变量需添加后缀（如`_`s），区分局部变量和全局变量如 system_status_s。
- 指针命名需添加后缀（如_p）区分局部变量和全局变量如 system_status_p。

## 3\.2 函数命

- 函数名采用小写字母，多个单词用下划线分隔，如`calculate_sum`、`init_device`。

- 函数名需体现函数功能，采用“动词\+名词”的结构，如`read_data`（读取数据）、`set_config`（设置配置）。

## 3\.3 宏与常量命名

- 宏定义和常量采用大写字母，多个单词用下划线分隔，如`#define MAX_BUFFER_SIZE 1024`、`const int ERROR_CODE = -1`。

- 宏定义需添加括号，避免展开时出现优先级问题，如`#define ADD(a, b) ((a) + (b))`。

## 3\.4 结构体与枚举命名

- 结构体名采用小写字母，多个单词用下划线分隔，可添加后缀`_t`表示类型，如`struct user_info_t`。

- 枚举名采用小写字母，多个单词用下划线分隔，枚举值采用大写字母，如：
        `enum status_t {
    STATUS_OK,
    STATUS_ERROR,
    STATUS_TIMEOUT
    };`

# 四、注释规范

## 4\.1 注释原则

注释需清晰、简洁，说明代码的“为什么”而非“是什么”，避免冗余注释。关键逻辑、复杂算法、特殊处理等必须添加注释。

## 4\.2 单行注释

- 使用`//`进行单行注释，注释内容与代码之间需加空格，如`// 初始化系统时钟`。

- 单行注释可放在代码上方或右侧，右侧注释需与代码对齐，避免混乱。

## 4\.3 多行注释

- 使用`/* */`进行多行注释，适用于函数说明、模块说明等，示例：
  
    ```c
    /*
     * 函数功能：计算两个整数的和
     * 参数：a - 第一个整数，b - 第二个整数
     * 返回值：两个整数的和
     */
    int add(int a, int b) 
    {
       return a + b;
    }
    ```
    
    
## 4\.4 特殊注释

- 待优化、待修改的代码需添加`// TODO: 说明内容`，如`// TODO: 优化算法效率，减少时间复杂度`。

- 已知问题需添加`// FIXME: 说明内容`，如`// FIXME: 修复数组越界问题`。

# 五、代码结构规范

## 5\.1 头文件规范

- 头文件需添加头文件保护宏，避免重复包含，示例：

- ```c
  #ifndef USER_INFO_H
  #define USER_INFO_H
  
  // 头文件内容
  
  #endif // USER_INFO_H`
  ```

  

- 头文件中只声明函数、宏、结构体等，不实现具体逻辑，实现逻辑放在对应的源文件中。

- 头文件需包含必要的依赖头文件，避免源文件重复包含。

## 5\.2 源文件规范

- 源文件开头需添加文件说明，包括文件功能、作者、创建日期等信息，示例：
    
    ```c
    /*
     * 文件功能：系统初始化相关函数实现
     * 作者：XXX
     * 创建日期：2026-08-08
    */
    ```
    
    
- 函数实现需遵循“先声明、后实现”的原则，静态函数需在文件开头声明。

- 源文件中避免使用全局变量，如需使用，需限制作用域并添加清晰注释。

# 六、编程习惯规范

- 避免使用魔法数字，所有常量需用宏定义或 const 变量表示，如用`MAX_RETRY_COUNT`代替数字5。

- 变量使用前需初始化，避免未初始化变量导致的未知错误。

- 函数参数需进行合法性检查，避免无效参数导致程序崩溃。

- 避免嵌套过深的代码（建议不超过3层），可通过拆分函数、优化逻辑减少嵌套。

- 合理使用指针，避免空指针引用、野指针等问题，指针使用前需判空。

- 内存分配后需及时释放，避免内存泄漏，如`malloc`后需对应`free`。

# 七、附则

本规范由项目组负责维护和更新，如有疑问或建议，可向项目组提出并讨论修改。所有开发人员需严格遵守本规范，代码提交前需自行检查规范符合性。
